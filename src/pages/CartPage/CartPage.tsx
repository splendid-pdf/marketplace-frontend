import classes from './CartPage.module.scss';

const CartPage = () => {
  return (
    <div className={`${classes.CartPage} main`}>
      <h1>Корзина</h1>
    </div>
  );
};

export default CartPage;
