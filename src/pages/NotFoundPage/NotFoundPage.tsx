import classes from './NotFoundPage.module.scss';

export const NotFoundPage = () => {
  return (
    <div className={`${classes.NotFoundPage} main`}>
      <h1>Страница не найдена</h1>
    </div>
  );
};
